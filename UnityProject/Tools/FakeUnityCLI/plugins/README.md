# Project-owned FakeUnityCLI Plugins

This directory is owned by the Unity project. Put one manifest-bound Provider/alias package in each namespaced
subdirectory, for example `eftm.game-automation/manifest.json`. FakeUnityCLI deployment creates this root but never
overwrites or removes project packages.
