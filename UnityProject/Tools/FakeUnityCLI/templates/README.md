# Project-owned FakeUnityCLI Templates

This directory is owned by the Unity project. Put one standalone Template bundle in each namespaced subdirectory, for
example `eftm.core/manifest.json`. FakeUnityCLI deployment creates this root but never overwrites or removes project
bundles.
