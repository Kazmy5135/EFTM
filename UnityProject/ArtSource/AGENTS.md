# Blender 场景源文件工作规则

- Blender 建模、渲染、资源导出任务先读 `BlenderWorkflow.md`，并读取目标资产目录的说明。
- 优先通过已配置的 `blender` MCP 读取当前场景，再使用 `bpy` 实施局部修改；渲染预览后检查结果并迭代。
- 当前窗口有未保存修改时，禁止为了重连 MCP 而重启 Blender 或重新加载文件；保留内存中的工作。
- 批量生成和后台渲染使用独立进程及明确的工作副本；重跑生成脚本前检查它会覆盖哪些产物。
- 交付需检查真实几何、材质、尺度以及 Unity 游戏摄像机下的效果，不能只凭俯视渲染判断完成。
