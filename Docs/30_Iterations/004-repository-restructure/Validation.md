# 验证记录

## 基线绑定

- 日期：2026-08-22
- 工作项或变更编号：004
- 设计基线：不适用；本工作项不改变玩法设计
- 技术基线：`ADR-001`
- 实现 commit 或资产标识：本工作项提交，见 Git 历史

## 验收

| 验收项 | 方法 | 结果 | 证据 |
|---|---|---|---|
| 三工作区目录结构 | 文件系统检查 | 通过 | 根目录平级存在 `Docs/`、`H5Prototype/`、`UnityProject/`，旧 Unity 根目录不再残留 |
| Unity 项目根目录有效 | 检查版本和必要目录 | 静态通过 | `ProjectVersion.txt` 为 `2022.3.62f2`；`Assets`、`Packages`、`ProjectSettings` 齐全 |
| 框架校验 | Unity Editor 菜单校验 | 待复验 | 当前机器只有 Unity `6000.0.73f1`；未用高版本打开或升级工程 |
| EditMode 测试 | Unity Test Framework | 待复验 | 迁移前 2026-08-18 记录为 3/3 通过；迁移后尚未在 `2022.3.62f2` 重跑 |
| H5 为可选分支 | 检查根、Docs、H5、Unity agent 规则 | 通过 | 四级 `AGENTS.md` 和三个角色文档均明确 H5 仅由设计者按需触发 |
| Git 迁移完整性 | HEAD 与新路径逐文件哈希、`.meta` 计数 | 通过 | 46 个跟踪文件全部存在；45 个逐字节一致，校验器 1 个为预期路径修订；`.meta` 为 16/16 |
| 文档路径与链接 | 搜索旧根路径并检查 Markdown 相对链接 | 通过 | 未发现断开的相对 Markdown 链接；Unity 内部 `Assets/...` 路径按工程相对路径保留 |

## 同步检查

- [x] 当前基线已同步。
- [x] 未并列保留互相冲突的新旧结论。
- [x] 链接、格式和标识检查通过。

## 未通过项

- 无失败项。
- 待使用 Unity `2022.3.62f2` 执行迁移后的框架校验和 EditMode 测试；不得用已安装的 Unity 6000 代替，以免升级工程。
