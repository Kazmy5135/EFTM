# 任务列表

## 待处理

- [ ] 安装或使用 Unity `2022.3.62f2`，从新路径执行框架校验。
- [ ] 使用 Unity `2022.3.62f2` 重新运行现有 EditMode 测试。

## 进行中

- 无。

## 已完成

- [x] 核对工作区状态并保留现有未提交文档。
- [x] 将完整 Unity 工程迁移到 `UnityProject/`。
- [x] 建立 `H5Prototype/` 可选原型工作区。
- [x] 建立根级和三个工作区的 agent 路由。
- [x] 更新项目入口、agent 规则、ADR 和追踪文档。
- [x] 完成目录、文件哈希、`.meta`、路径、Markdown 链接和 diff 静态检查。

## 权威同步清单

- [x] 所有必须修改的当前基线已更新。
- [x] 正式正文只保留当前结论。
- [x] 工作项、Git commit 和追踪登记已同步。

## 阻塞

- 当前机器只发现 Unity `6000.0.73f1`，未发现项目要求的 `2022.3.62f2`。为避免工程升级，尚未执行迁移后的 Editor 框架校验和 EditMode 测试。
