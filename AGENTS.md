# AGENTS.md - EFTM 项目入口

## 项目结构与任务路由

本仓库包含三个平级工作区。开始任务前先判断工作类型，再进入对应目录读取局部 `AGENTS.md`：

| 工作类型 | 工作根目录 | 局部规则 |
|---|---|---|
| 产品范围、玩法规则、策划案、设计评审、研究与项目治理 | `Docs/` | `Docs/AGENTS.md` |
| 设计者明确要求的 H5 体感或交互验证 | `H5Prototype/` | `H5Prototype/AGENTS.md` |
| Unity 代码、场景、Prefab、资源、编辑器工具、测试与构建 | `UnityProject/` | `UnityProject/AGENTS.md` |

混合任务先拆清设计语义、验证证据和正式实现，不让任一工作区静默覆盖另一工作区的事实。

## H5 原型的触发规则

- H5 是可选验证工具，不是设计或 Unity 开发的通用门禁。
- 只有设计者明确提出需要通过 H5 验证体感、交互或可用性时，才进入 `H5Prototype/`。
- 不得仅因仓库存在 H5 工程或 H5 agent，就自动创建原型。
- 没有 H5 原型，不影响满足其他门禁的设计进入评审、批准和 Unity 开发。
- 只有具体工作项明确把 H5 验证列为验收项时，它才是该工作项的局部门禁。
- H5 结果是验证证据，不自动成为正式设计结论；结论由策划设计流程确认。

## 权威来源与交接

- 设计规则、项目状态和验收标准：`Docs/`。
- H5 当前实现与实验结果：`H5Prototype/` 及对应工作项的验证记录。
- Unity 当前实现事实：`UnityProject/` 中的代码、资产和测试。
- 文档与实现冲突时必须报告冲突，不得通过静默改写任一方掩盖问题。
- 策划到 H5、策划到 Unity、H5 回传策划以及 Unity 回传验证的交接字段见 `Docs/00_Project/Agents/README.md`。

## 通用工作规则

- 开始前检查 `git status`，保留用户和其他人的无关改动。
- 诊断、审查和解释任务默认只读；用户明确要求实施后才能修改。
- 使用 UTF-8 读写中文文件，避免无关格式变化。
- 只读取完成当前任务需要的入口、直接依赖、测试与权威文档，不默认扫描整个仓库。
- 完成前运行与风险相称的最小验证，并检查 `git diff` 和 `git status`。
- 除非用户明确要求，不执行提交、推送、删除或大范围清理。

## 用量重置授权

- 在任何对话中使用用户的用量重置次数前，必须先向用户申请并获得针对本次重置的明确批准。
- 不得自动使用重置次数；一般任务授权、继续工作或此前其他重置的批准不等于本次重置授权。

## 图片交付

- 聊天中需要展示的图片统一保存到仓库根目录 `codex-chat-images/`。
- 使用该图片的本地绝对路径嵌入消息，不依赖可能失效的远程图片链接。
- `codex-chat-images/` 是本地临时目录，不提交到 Git。
